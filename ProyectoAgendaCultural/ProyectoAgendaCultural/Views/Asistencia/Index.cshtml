@model IEnumerable<ProyectoAgendaCultural.Models.Asistencia>

@{
    ViewBag.Title = "Index";
}

<link href="~/Content/estilo_menuVertical.css" rel="stylesheet" />
<br />
<br />
<br />
<br />
<div class="container">
    <div class="row">
        <div class="col-sm-3">
            <!--Menu de administrador-->
            @{Html.RenderPartial("_menuVertical");}
            <hr class="hidden-sm hidden-md hidden-lg">
        </div>
        <div class="col-sm-9">
            <div class="panel panel-info">
                <div class="panel-heading">
                    <h2 style="color:black">Listado de asistencia</h2>
                </div>
                <div class="panel-body">

                    <p>
                        @Html.ActionLink("Registrar nueva asistencia", "Create")
                    </p>
                    <table class="table">
                        <tr>
                            <th>
                                Nombre evento
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Participante.Nombres)
                            </th>
                            <th></th>
                        </tr>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Evento.Nombre)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Participante.NombreCompleto)
                                </td>
                                <td>
                                    @Html.ActionLink("Editar", "Edit", new { id = item.Id }) |
                                    @Html.ActionLink("Borrar", "Delete", new { id = item.Id })
                                </td>
                            </tr>
                        }

                    </table>
                </div>
            </div>
        </div>
    </div>
</div>