@model IEnumerable<ProyectoAgendaCultural.Models.ClasesSP.EventosDeArtista>

@{
    ViewBag.Title = "EventoArtista";
}

<h2>EventoArtista</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_evento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Imagen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_evento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha_evento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_evento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Imagen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_evento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha_evento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_artista }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_artista }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_artista })
        </td>
    </tr>
}

</table>
